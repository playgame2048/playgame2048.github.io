<!-- ===================== style.css (CSS FILE) ===================== -->
<style>
* { margin: 0; padding: 0; box-sizing: border-box; }
body {
  font-family: Arial, sans-serif;
  background: #0b0d12;
  color: #fff;
  display: flex;
  justify-content: center;
  padding: 20px;
}

.container { max-width: 500px; width: 100%; }
header { text-align: center; margin-bottom: 20px; }
header h1 { font-size: 2rem; margin-bottom: 5px; }
.subtitle { opacity: 0.8; font-size: 0.9rem; }

.controls {
  display: flex;
  justify-content: space-between;
  margin-bottom: 15px;
}
button {
  padding: 10px 16px;
  border: none;
  border-radius: 8px;
  cursor: pointer;
  font-size: 14px;
  font-weight: bold;
}
#restartBtn { background: #4caf50; color: #fff; }
#themeBtn { background: #222; color: #fff; }

/* Sudoku Grid */
#game {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 5px;
  margin-bottom: 15px;
}
.cell {
  width: 80px;
  height: 80px;
  background: #1e1e1e;
  border-radius: 10px;
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 2rem;
  cursor: pointer;
  transition: 0.2s;
}
.cell:focus { outline: 2px solid #4caf50; }
.cell.prefilled { background: #333; cursor: default; }

#message { text-align: center; font-size: 1.2rem; margin-top: 10px; }

.blog { margin-top: 25px; padding: 15px; background: #111; border-radius: 10px; }
.blog h2 { margin-bottom: 10px; }
.blog p { opacity: 0.9; line-height: 1.4; }

/* DARK MODE */
.dark body { background: #fff; color: #000; }
.dark .cell { background: #eee; color: #000; }
.dark .prefilled { background: #ccc; }
.dark .blog { background: #f3f3f3; color: #000; }
</style>
